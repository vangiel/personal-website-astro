---
interface Propos {
	className: string;
	date: Date;
}

const { className, date } = Astro.props;
---

<time datetime={date.toISOString()} class={className}
	>{
		date.toLocaleDateString(undefined, {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}</time
>
